{
  "permissions": {
    "allow": [
      "Bash(just check)",
      "Bash(just lint-llm-diff origin/master --skip-nx-cache)",
      "Bash(git status --short --branch)",
      "Bash(git diff --check)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./scripts/ci/session-setup.sh # llmlint: ignore[changed_behavior_has_e2e] This developer session hook has no browser interface and its real subprocess boundaries are covered by tooling-ci specs."
          }
        ]
      }
    ]
  }
}
