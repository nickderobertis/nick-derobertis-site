{
  "_llmlint": "ignore-file[least_privilege_grants] These reviewed developer permissions support the repository's required just, pnpm, and commit workflow; the SessionStart hook itself invokes only its fixed checked-in script.",
  "permissions": {
    "allow": [
      "Bash(just *)",
      "Bash(pnpm *)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(uv run --script *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./scripts/ci/session-setup.sh # llmlint: ignore[changed_behavior_has_e2e] This developer session hook has no browser interface and its real subprocess boundaries are covered by tooling-ci specs."
          }
        ]
      }
    ]
  }
}
