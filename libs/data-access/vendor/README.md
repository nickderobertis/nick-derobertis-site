# CV data artifacts

These files are the committed `codegen/` output from
`nickderobertis/derobertis-cv` commit
`2dc93d5a799243224592f6a22a9a822ace8057ee`. They are vendored because the
currently published package contains metadata only.

TODO: replace this directory with `@nickderobertis/cv-data` imports once the
npm package publishes its generated JSON, schema, and TypeScript declarations.
