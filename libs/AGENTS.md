# Shared library rules

Keep libraries framework-light and independently typed. Validate external values in `data-access-core`; never fetch directly from views. Respect the tag boundaries in the root ESLint config.
