# Shell rules

The shell owns the Pages base path, routing, shared layout, and remote composition. Keep feature content out of this app; remotes mount at route boundaries and may consume or expose federated modules.
