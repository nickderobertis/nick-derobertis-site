{
  "name": "shell",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/shell/src",
  "projectType": "application",
  "tags": ["type:app", "scope:shell"],
  "targets": {
    "e2e": {
      "metadata": {
        "description": "llmlint: ignore[browser_journeys_run_against_the_built_app] This target is not a browser journey added inside an app project: it drives the built artifact, never a development server, because it depends on shell:prerender, which composes and gates the production bundle before Playwright is started. It costs an unrelated change nothing, because nx affected schedules it only when shell itself is affected. This branch adds no journey here; it wires the bundle-budget gate into the prerender this target already depended on, and AGENTS.md records that every app in this workspace owns the journeys for its own standalone and host-composed boundary."
      },
      "executor": "@nx/playwright:playwright",
      "dependsOn": [{ "target": "prerender", "projects": ["shell"] }],
      "options": {
        "config": "apps/shell/e2e/playwright.config.ts",
        "skipInstall": true
      }
    },
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": [
        "{options.outputPath}",
        "{options.outputPath}/fragment.html",
        "{options.outputPath}/fragment.css",
        "{options.outputPath}/fragment.json"
      ],
      "options": {
        "rspackConfig": "apps/shell/rspack.config.ts",
        "outputPath": "dist/apps/shell",
        "index": "apps/shell/src/index.html",
        "main": "apps/shell/src/main.tsx",
        "tsConfig": "apps/shell/tsconfig.app.json",
        "assets": []
      }
    },
    "serve": {
      "executor": "@nx/rspack:dev-server",
      "options": { "buildTarget": "shell:build", "port": 4200 }
    },
    "prerender": {
      "executor": "nx:run-commands",
      "metadata": {
        "description": "llmlint: ignore[changed_behavior_has_e2e] Nx dependency selection has no browser interface and remote-manifest.spec.ts exercises it at the project-graph contract; site.spec.ts drives the composed artifact with JavaScript disabled and through hydration, and every journey spec drives standalone and host-composed boundaries."
      },
      "options": {
        "command": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/compose/compose.mjs && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/artifact/check-static-artifact.mjs && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/artifact/check-bundle-budgets.mjs # llmlint: ignore[changed_behavior_has_e2e] Compose contract and filesystem failures happen before an artifact can be served, so compose.spec.ts exercises that real CLI boundary; site.spec.ts drives its successful output with and without JavaScript."
      }
    },
    "perf": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "node scripts/perf/performance-audit.mjs"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "metadata": {
        "description": "eslint runs once for the whole workspace here, so this target's cache key covers every file it reads: its own configuration, every project's TypeScript and JavaScript, and every project.json, because @nx/enforce-module-boundaries reads the tags they declare. Keyed on apps/shell alone, a cached pass could be replayed for a rule or a source file that no run ever saw."
      },
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/biome.json",
        "{workspaceRoot}/eslint.config.mjs",
        "{workspaceRoot}/**/*.ts",
        "{workspaceRoot}/**/*.tsx",
        "{workspaceRoot}/**/*.js",
        "{workspaceRoot}/**/*.jsx",
        "{workspaceRoot}/**/*.mjs",
        "{workspaceRoot}/**/*.cjs",
        "{workspaceRoot}/**/project.json"
      ],
      "options": {
        "command": "eslint . --max-warnings=0 && biome lint apps/shell"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/shell/tsconfig.app.json && tsc -p apps/shell/e2e/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/apps/shell"],
      "options": {
        "command": "vitest run --config apps/shell/vite.config.ts --coverage"
      }
    }
  }
}
