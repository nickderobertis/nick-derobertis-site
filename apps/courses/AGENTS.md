# Courses rules

This remote publishes the `/courses` route page.

## Journeys

1. The course list and its topics render from CV data, and full and sparse
   course records stay distinguishable, through both the standalone remote and
   the host-composed route.
2. Its loading, empty, and error states are exposed through both render paths.
3. Course panes respond from mobile through desktop through both render paths.
