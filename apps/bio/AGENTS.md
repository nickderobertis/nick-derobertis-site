# Bio rules

This remote publishes the `/bio` route page and the complete biography.

## Journeys

1. The complete biography renders, its loading, empty, and error states are
   exposed, and the layout matches at each viewport, through both the standalone
   remote and the host-composed route.
2. The legacy `/story` route redirects to the host-composed biography.
