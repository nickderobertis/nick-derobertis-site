{
  "name": "home-cards",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/home-cards/src",
  "projectType": "application",
  "tags": ["type:remote", "scope:home-cards"],
  "targets": {
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "rspackConfig": "apps/home-cards/rspack.config.ts",
        "outputPath": "dist/apps/home-cards",
        "main": "apps/home-cards/src/main.tsx",
        "tsConfig": "apps/home-cards/tsconfig.app.json",
        "index": "apps/home-cards/src/index.html",
        "assets": []
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "biome lint apps/home-cards" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p apps/home-cards/tsconfig.app.json" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests apps/home-cards/src"
      }
    },
    "screenshot": {
      "executor": "nx:run-commands",
      "options": { "command": "scripts/visual-project.sh home-cards" }
    }
  }
}
