{
  "name": "home-contact",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/home-contact/src",
  "projectType": "application",
  "tags": ["type:remote", "scope:home-contact"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "dependsOn": [{ "target": "prerender", "projects": ["shell"] }],
      "options": {
        "command": "E2E_REMOTE=home-contact E2E_PORT=4314 playwright test --config apps/shell-e2e/playwright.config.ts remote-owner.spec.ts home.spec.ts"
      }
    },
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": [
        "{options.outputPath}",
        "{options.outputPath}/fragment.html",
        "{options.outputPath}/fragment.css",
        "{options.outputPath}/fragment.json"
      ],
      "options": {
        "rspackConfig": "apps/home-contact/rspack.config.ts",
        "outputPath": "dist/apps/home-contact",
        "main": "apps/home-contact/src/main.tsx",
        "tsConfig": "apps/home-contact/tsconfig.app.json",
        "index": "apps/home-contact/src/index.html",
        "assets": []
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "biome lint apps/home-contact" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p apps/home-contact/tsconfig.app.json" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests apps/home-contact/src"
      }
    },
    "screenshot": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/visual/capture-visual.mjs home-contact \"${SHOTS_OUT:-shots/current/home-contact/x86_64}\""
      }
    }
  }
}
