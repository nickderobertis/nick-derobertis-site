{
  "name": "software",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/software/src",
  "projectType": "application",
  "tags": ["type:remote", "scope:software"],
  "metadata": {
    "federation": { "alias": "software" },
    "boundaries": {
      "onlyDependOnLibsWithTags": [
        "type:shared",
        "type:data-core",
        "data:software"
      ]
    }
  },
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "dependsOn": [{ "target": "prerender", "projects": ["shell"] }],
      "options": {
        "command": "playwright test --config apps/software/e2e/playwright.config.ts"
      }
    },
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": [
        "{options.outputPath}",
        "{options.outputPath}/fragment.html",
        "{options.outputPath}/fragment.css",
        "{options.outputPath}/fragment.json"
      ],
      "options": {
        "rspackConfig": "apps/software/rspack.config.ts",
        "outputPath": "dist/apps/software",
        "main": "apps/software/src/main.tsx",
        "tsConfig": "apps/software/tsconfig.app.json",
        "index": "apps/software/src/index.html",
        "assets": []
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "biome lint apps/software" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/software/tsconfig.app.json && tsc -p apps/software/e2e/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config apps/software/vite.config.ts --coverage"
      }
    },
    "screenshot": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON apps/software/visual/capture.ts \"${SHOTS_OUT:-shots/current/software/x86_64}\""
      }
    },
    "serve": {
      "executor": "@nx/rspack:dev-server",
      "options": {
        "buildTarget": "software:build",
        "port": 4200,
        "host": "127.0.0.1"
      }
    }
  }
}
