# Software rules

This remote publishes the `/software` route page.

## Journeys

1. The project grid, its optional fields, and the project totals render through
   both the standalone remote and the host-composed route.
2. Its loading, empty, and error states are exposed through both render paths.
3. The project grid responds from mobile through desktop through both render
   paths.
