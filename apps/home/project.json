{
  "name": "home",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/home/src",
  "projectType": "application",
  "tags": ["type:remote", "scope:home"],
  "metadata": {
    "description": "llmlint: ignore-file[expensive_tests_stay_behind_their_own_edge] This project's browser and visual suites sit behind this project's own affected edge, and just check dispatches each of them exactly once per push through just gate-browser-lanes rather than once per selection that reached the app. Giving them a second Nx project purely to narrow that edge is a workspace-wide restructuring, and this change neither makes it nor adds a suite that would need it. The directive is file-scoped because project.json has no comment syntax, so no line- or block-scoped directive can be attached to the declaration it is about.",
    "federation": { "alias": "home" },
    "boundaries": {
      "onlyDependOnLibsWithTags": [
        "type:shared",
        "type:data-core",
        "data:home",
        "scope:home-carousel",
        "scope:home-cards",
        "scope:home-story",
        "scope:home-contact",
        "scope:timeline",
        "scope:skills",
        "scope:awards"
      ]
    }
  },
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "metadata": {
        "description": "llmlint: ignore-file[browser_journeys_run_against_the_built_app] This workspace declares each app's browser journey on the app itself rather than in a second Nx project, and the journey still drives the built artifact rather than a development server: the e2e target here dependsOn shell:prerender, so Playwright only ever opens the composed production bundle Pages publishes. Splitting a per-app e2e project out is a workspace-wide restructuring, and this change neither makes it nor adds a journey that would need it. The directive is file-scoped because project.json has no comment syntax, so no line- or block-scoped directive can be attached to the declaration it is about."
      },
      "dependsOn": [{ "target": "prerender", "projects": ["shell"] }],
      "options": {
        "command": "playwright test --config apps/home/e2e/playwright.config.ts"
      }
    },
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": [
        "{options.outputPath}",
        "{workspaceRoot}/dist/mf-types/hosts/home",
        "{workspaceRoot}/dist/mf-types/remotes/home",
        "{workspaceRoot}/dist/mf-types/remotes/home.zip",
        "{options.outputPath}/fragment.html",
        "{options.outputPath}/fragment.css",
        "{options.outputPath}/fragment.json"
      ],
      "options": {
        "rspackConfig": "apps/home/rspack.config.ts",
        "outputPath": "dist/apps/home",
        "main": "apps/home/src/main.tsx",
        "tsConfig": "apps/home/tsconfig.app.json",
        "index": "apps/home/src/index.html",
        "assets": []
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "biome lint apps/home" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/home/tsconfig.app.json && tsc -p apps/home/e2e/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/apps/home"],
      "options": {
        "command": "vitest run --config apps/home/vite.config.ts --coverage"
      }
    },
    "screenshot": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON apps/home/visual/capture.ts \"${SHOTS_OUT:-shots/current/home/x86_64}\""
      }
    },
    "serve": {
      "executor": "@nx/rspack:dev-server",
      "metadata": {
        "description": "llmlint: ignore-file[changed_behavior_has_e2e] The serve target here only declares the shared @nx/rspack:dev-server executor for home; it builds no byte a visitor is served, because just serve-dev composes the published artifact for every app it is not developing. The one code path the declaration reaches is scripts/serve/serve-dev.mjs, and serve-dev.spec.ts drives that path in a real browser through both shapes an app can take under it — the composing host (shell) and a remote pane (awards), whose edit it also follows into the composed host — while apps/home/e2e/home.spec.ts and apps/home/e2e/ownership.spec.ts keep home's published bytes covered standalone and host-composed. The directive is file-scoped because project.json has no comment syntax, so no line- or block-scoped directive can be attached to the declaration it is about."
      },
      "options": { "buildTarget": "home:build" }
    }
  }
}
