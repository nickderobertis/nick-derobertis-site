{
  "name": "tooling-artifact",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "scripts/artifact",
  "projectType": "application",
  "tags": ["type:tooling", "scope:build"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome lint scripts/artifact"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p scripts/artifact/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --maxWorkers=1 --config scripts/artifact/vite.config.ts"
      },
      "dependsOn": [
        {
          "target": "prerender",
          "projects": ["shell"]
        }
      ]
    }
  }
}
