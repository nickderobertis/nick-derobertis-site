import { SkillStatisticsModel } from './skill-statistics-model';

describe('SkillStatisticsModel', () => {
  it('should create an instance', () => {
    expect(new SkillStatisticsModel()).toBeTruthy();
  });
});
