import { HomeCarouselItemModel } from './home-carousel-item-model';

describe('HomeCarouselItemModel', () => {
  it('should create an instance', () => {
    expect(new HomeCarouselItemModel()).toBeTruthy();
  });
});
