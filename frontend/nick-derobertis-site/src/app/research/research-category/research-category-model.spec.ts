import { ResearchCategoryModel } from './research-category-model';

describe('ResearchCategoryModel', () => {
  it('should create an instance', () => {
    expect(new ResearchCategoryModel()).toBeTruthy();
  });
});
