import { SoftwareProjectModel } from './software-project-model';

describe('SoftwareProjectModel', () => {
  it('should create an instance', () => {
    expect(new SoftwareProjectModel()).toBeTruthy();
  });
});
