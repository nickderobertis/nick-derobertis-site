import { SoftwarePageModel } from './software-page-model';

describe('SoftwarePageModel', () => {
  it('should create an instance', () => {
    expect(new SoftwarePageModel()).toBeTruthy();
  });
});
