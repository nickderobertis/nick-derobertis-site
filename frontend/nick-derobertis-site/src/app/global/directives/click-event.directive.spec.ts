import { ClickEventDirective } from './click-event.directive';

describe('ClickEventDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEventDirective();
    expect(directive).toBeTruthy();
  });
});
